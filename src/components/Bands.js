import React, { Component } from 'react';
import Band from './Band';


class Bands extends Component {

    return(
      <ul>
        {/* map through all the bands and input the const here*/}
      </ul>
    );
  }
};

export default Bands;
